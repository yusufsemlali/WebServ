#pragma once

// # include "config.hpp"
// # include "error.hpp"
// # include "webserv.hpp"
# include "parser.hpp"
#include <iostream>

typedef struct s_data
{
	// t_error		error;
	// t_config	config;
}				t_data;

void core(void);
