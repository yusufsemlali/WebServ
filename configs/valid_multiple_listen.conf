# Valid Configuration: Multiple different listen directives
# Should pass: different IP:port combinations in same server block

server {
    listen 9081;
    listen 9082;
    listen 127.0.0.1:9090;
    listen 0.0.0.0:9093;
    root ./www;
}
